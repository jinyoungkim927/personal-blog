// Feature flags for the site (CommonJS version for gatsby-node.js)
// Note: Keep in sync with features.ts
const features = {
  graphEnabled: false, // Set to true to enable graph features
}

module.exports = features
module.exports.default = features
