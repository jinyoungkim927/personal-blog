// Feature flags for the site
// Note: Keep in sync with features.js (used by gatsby-node.js)
export const features = {
  graphEnabled: false, // Set to true to enable graph features
}

export default features
