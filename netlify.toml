[build]
  command = "npm run build"
  publish = "public"

[build.environment]
  NODE_VERSION = "18"

# Homepage redirects to blog (same content)
[[redirects]]
  from = "/"
  to = "/blog/"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
